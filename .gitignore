# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Node / Next.js
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/next-env.d.ts
frontend/.env*.local

# Env
.env

# OS / editors
.DS_Store
*.swp
.vscode/
.idea/

# Claude Code project guidance (local only, not part of the shipped project)
/CLAUDE.md

# Private ops runbook - server-specific (domain, paths, IPs), not meant
# for the public repo
/docs/deploy-hetzner.md

# Server-local Docker Compose override (e.g. binding ports to 127.0.0.1
# behind a reverse proxy) - see docs/deploy-hetzner.md
docker-compose.override.yml
